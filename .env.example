# Kam se stahují CSV data
DATA_DIR=./data

# Kam se generují JSONy pro Hugo (cesta k hugo portálu)
OUTPUT_DIR=./site/static/data/charts

# Databáze pro load_to_db.py a čtení v generate_json.py.
# Heslo níže je jen pro lokální vývoj — v produkci jde ze secretů portálu.
DB_HOST=localhost
DB_PORT=5432
POSTGRES_DB=pathogens
POSTGRES_USER=portal
POSTGRES_PASSWORD=portal_dev
